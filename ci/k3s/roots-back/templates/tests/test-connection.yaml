apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "roots-back.fullname" . }}-test-connection"
  labels:
    {{- include "roots-back.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "roots-back.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
